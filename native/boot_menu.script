# 起動の言語メニューをそのまま見る（言語は選ばない）
# ★頭は ENGLISH が選ばれている（原典の言語）
#
# ★★1 行も無い台本にすると、SDL 版は **ZENMAI_STOP が発火せず固まる**（`sc_n == 0`）。
#   だから「何も起きない行」を 1 つだけ置く —— SELECT は言語メニューでは無視されるし、
#   9000 回目は撮りたい場面よりずっと後なので、押されること自体が無い。
9000-9001:SEL
