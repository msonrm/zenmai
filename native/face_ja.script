# ★言語を**面ボタン**で選ぶ台本。既存の台本は全部 Start で選んでいたので、
#   この経路は誰も踏んでいなかった（2026-08-29・実機 R36H で発覚）。
#
# ✕ を長めに押したまま「日本語」を決める。押されたままゲームへ入るので、
# 引き継ぎ（carry_over_pad）が無いと **✕ に応じた「お」がコマンド欄に入る**。
# ★既定は ENGLISH（原典の言語）なので、先に ↓ で 日本語 へ移る。
60-70:DOWN
100-240:X
